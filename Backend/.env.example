# Copy to .env and replace <db_password> with your Atlas user's password
# Example MongoDB Atlas connection string (set the real password in <db_password>)
MONGODB_URI=mongodb+srv://nikhiljomon0505_db_user:<db_password>@cluster0.qig0wwz.mongodb.net/attendancetracker?retryWrites=true&w=majority
# Local fallback (uncomment to use local MongoDB instead)
# MONGODB_URI=mongodb://127.0.0.1:27017/attendancetracker

# Server port
PORT=5000
